@{
    ViewData["Title"] = "Supporting Documents";
    ViewData["HeaderText"] = "Online Application";
    ViewData["ShowStepper"] = true;
    ViewData["CurrentStep"] = 4;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Load Font Awesome -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
<link href="~/css/page4.css" rel="stylesheet" />
<body class="" style="overflow-x: hidden;">
<div class="container px-4 px-md-5 my-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-10 col-lg-9">

            <h5 class="fw-semibold text-primary mb-4 text-center text-md-start">
                Find out the supporting documents you will need to complete your online application!
            </h5>

            <div class="row g-3  align-items-stretch">
                <!-- Tab 1 -->
                <div class="col-12 col-md-6 col-lg-3">
                    <div class="card tab-card border-primary text-primary rounded-3 shadow-sm py-3 active" data-target="#onName">
                        <div class="card-body text-center">
                            <h6 class="fw-bold mb-1">Change provider</h6>
                            <small>The meter is in my name</small>
                        </div>
                    </div>
                </div>

                <!-- Tab 2 -->
                <div class="col-12 col-md-6 col-lg-3">
                    <div class="card tab-card border-primary text-primary rounded-3 shadow-sm py-3 " data-target="#anotherName">
                        <div class="card-body text-center">
                            <h6 class="fw-bold mb-1">Change provider</h6>
                            <small>on another name</small>
                        </div>
                    </div>
                </div>

                <!-- Tab 3 -->
                <div class="col-12 col-md-6 col-lg-3">
                    <div class="card tab-card border-primary text-primary rounded-3 shadow-sm py-3" data-target="#initialElectrification">
                        <div class="card-body text-center">
                            <h6 class="fw-bold mb-1">Initial Electrification</h6>
                            <small>New connection</small>
                        </div>
                    </div>
                </div>

                <!-- Tab 4 -->
                <!-- Tab 4: Re-electrification -->
                <div class="col-12 col-md-6 col-lg-3">
                    <div class="card tab-card border-primary text-primary rounded-3 shadow-sm py-3" data-target="#reElectrification">
                        <div class="card-body text-center">
                            <h6 class="fw-bold mb-0">Re-electrification</h6>
                        </div>
                    </div>
                </div>

            </div>


            <!-- Dynamic Content Panels -->
            <div id="onName">
                @Html.Partial("_ChangeProviderOnNamePartial")
            </div>
            <div id="anotherName" style="display: none;">
                @Html.Partial("ChangeProviderOnAnotherName")
            </div>
            <div id="initialElectrification" style="display: none;">
                @Html.Partial("_InitialElectrificationPartial")
            </div>
            <div id="reElectrification" style="display: none;">
                @Html.Partial("_ReElectrificationPartial")
            </div>

        </div>
    </div>
</div>
</body>

@Html.Partial("_NavigationBtnPartial")

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.querySelectorAll('.tab-card').forEach(card => {
            card.addEventListener('click', function () {
                document.querySelectorAll('.tab-card').forEach(c => c.classList.remove('active'));
                this.classList.add('active');

                // Hide all sections
                ['#onName', '#anotherName', '#initialElectrification', '#reElectrification'].forEach(id => {
                    document.querySelector(id).style.display = 'none';
                });

                // Show target section
                const target = this.getAttribute('data-target');
                if (target) {
                    document.querySelector(target).style.display = 'block';
                }
            });
        });
    </script>
}
